---

- name: Roles shared by every host
  hosts: all
  vars_files:
    - vars.yaml
  remote_user: "{{ ssh_user }}"
  roles:
    - base

- name: Snowflake role
  hosts: baremetal
  vars_files:
    - vars.yaml
  remote_user: "{{ ssh_user }}"
  roles:
    - snowflake